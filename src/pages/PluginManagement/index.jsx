import React from "react";
import { render } from "react-dom";
import PluginManagementPage from "./PluginManagementPage";

// Render the component to the DOM
render(<PluginManagementPage />, document.getElementById("root")); 